version: "3.8"
services:
  web:
    image: nginx:alpine
    container_name: demo-static-site
    ports:
      - "80:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
    restart: unless-stopped
